# 开发日志

## 需求分析

目标是从 0 到 1 实现一个 Web 单机小游戏 MVP：麻将胡牌是胜利目标，自走棋式商店、经济、羁绊、海克斯、城邦构成构筑循环。

第一版明确不做联网、后端、登录、排行榜、真实 IP 素材、复杂番型、吃碰杠和完整四人摸打。

## 方案设计

技术栈：

- React + TypeScript + Vite
- Zustand
- Tailwind CSS
- Framer Motion
- Vitest

模块边界：

- `data`：原创静态配置。
- `engine`：纯规则模块，便于测试。
- `store`：串联游戏动作。
- `components/pages`：展示与交互。

## 任务拆解

1. 初始化工程和测试工具。
2. 先写胡牌、商店、羁绊、AI、主流程测试。
3. 实现类型、静态配置和 engine。
4. 实现 Zustand store。
5. 实现首页、游戏页、规则页和弹窗。
6. 使用 frontend-design 准则优化视觉。
7. 补 README、规则文档和开发日志。
8. 运行测试、构建和浏览器 smoke test。

## 已完成内容

- 建立 Vite React TypeScript 工程。
- 实现 `TileDefinition`、`PlayerState`、`GameState` 等核心类型。
- 实现原创牌、羁绊、海克斯、城邦配置。
- 实现胡牌判定：
  - 基础胡牌
  - 七对子
  - 碰碰胡
  - 清一色
  - 简化距离估算
- 实现商店、利息、等级概率。
- 实现羁绊统计和刷新折扣。
- 实现海克斯选择和 AI 选择。
- 实现城邦随机与修正。
- 实现伤害结算和淘汰。
- 实现基础 AI。
- 实现首页、主界面、规则页、海克斯弹窗、结算弹窗。
- 第二版联机 MVP：
  - 新增 `shared/protocol` 和 `shared/types`。
  - 新增 Socket.IO + Express 服务端。
  - 新增 RoomManager、GameSessionManager、ConnectionManager。
  - 新增创建房间、加入房间、准备、开始游戏。
  - 新增服务端权威游戏会话和私有视图过滤。
  - 新增购买、刷新、锁店、弃牌、出售、选择海克斯、结束回合服务端动作。
  - 新增 60 秒重连窗口。
  - 新增服务端倒计时和超时结算入口。
  - 新增联机入口页、房间大厅、联机游戏页。

## 测试结果

已覆盖：

- 胡牌判定测试
- 七对子测试
- 碰碰胡测试
- 清一色测试
- 商店刷新测试
- 羁绊统计测试
- AI 行为基础测试
- 游戏主流程 smoke test
- RoomManager 测试
- GameSessionManager 动作测试
- ConnectionManager 重连测试

最新验证请以命令输出为准：

```bash
npm test
npm run build
```

## 插件与替代执行说明

- 已按 Superpowers 流程进行需求分析、任务拆解、TDD 红绿验证、失败定位和完成前验证。
- 已按 frontend-design 准则进行界面方向设计：暗色背景、东方麻将元素、自走棋控制台质感、轻量动效。
- GitHub 参考用于工程分层和同类开源项目方向检索，没有直接复制代码或素材。

## 遗留问题

- 锁店当前主要阻止手动刷新，后续应实现严格的跨回合保留商店。
- AI 和玩家暂时共用简化商店流，后续可改为 AI 私有行动上下文。
- 海克斯部分效果当前作为标签和后续扩展入口，未全部做成强效果。
- 等级升级按钮和经验购买尚未接入 UI。
- 图鉴入口暂时复用规则页，后续应独立为图鉴页。
- 联机状态当前使用内存存储，服务重启会丢失房间。
- 断线超时后的 AI 托管策略仍较轻量，后续需要更完整的托管行为。
- 暂未接入 Redis、数据库、账号、匹配和排行榜。
